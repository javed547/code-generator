# ============================================================
# ORG GRADLE TEMPLATE — gradle.properties
# ============================================================
# Replace with your organisation's gradle.properties.
# Common org-specific entries:
#   - Nexus/Artifactory credentials (use env vars, not plain text)
#   - JVM args for Gradle daemon
#   - Org proxy settings
#   - Feature flags for org plugins
# ============================================================

org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

# ADD YOUR ORG-SPECIFIC PROPERTIES HERE
# nexusUrl=https://nexus.example.com
# nexusUsername=${NEXUS_USERNAME}
# nexusPassword=${NEXUS_PASSWORD}